name: "EA Trading Dashboard"
description: "v4.6.2 - Online status + iOS fixes + pagination + period filters"
version: "4.6.2"
slug: ea-dashboard
init: false
arch: [armhf, armv7, aarch64, amd64, i386]
ports: {8099/tcp: 8099}
webui: "http://[HOST]:[PORT:8099]"
ingress: true
ingress_port: 8099
panel_icon: mdi:chart-line
startup: application
boot: auto
options:
  webhook_secret: ""
schema:
  webhook_secret: password?
